# 구성 관리 및 IaC

## IaC 개요
IaC(Infrastructure as Code)는 인프라를 코드로 선언/관리하여 재현성과 일관성을 확보하는 방식입니다.

## 주요 서비스

### AWS CloudFormation
- 템플릿(YAML/JSON) 기반 선언형 IaC
- Stack / Change Set / Drift Detection 활용
- StackSets로 멀티 계정/리전 배포

### AWS CDK
- TypeScript/Python/Java 등으로 인프라 코드 작성
- 고수준 구성(Construct)으로 생산성 향상
- 최종적으로 CloudFormation 템플릿으로 합성

### AWS Systems Manager
- Parameter Store: 구성값/시크릿 관리
- State Manager: 구성 상태 유지
- Automation Runbook: 운영 작업 자동화
- Session Manager: 안전한 접속

## 구성 관리 핵심 원칙
- **불변성(Immutable Infrastructure)** 우선
- **버전 관리(Git)**로 변경 추적
- **표준화된 모듈/템플릿 재사용**
- **환경 분리(Dev/Stg/Prod)**
- **정책 기반 거버넌스**(SCP, Config Rule)

## 배포 전략
- Blue/Green, Canary, Rolling 전략을 워크로드 특성에 맞게 선택
- 변경 전 Change Set 리뷰
- 실패 시 자동 롤백/알림 연계

## 보안 포인트
- 최소 권한 IAM 역할
- KMS로 데이터/시크릿 암호화
- CloudTrail 및 Config로 감사 가능성 확보
- 교차 계정 파이프라인 시 신뢰 정책 검토

## 시험 포인트
- CloudFormation Stack 실패 원인 분석(권한/의존성/리소스 제한)
- Drift 탐지 및 수정 방식
- SSM Parameter Store vs Secrets Manager 선택 기준
- CDK와 CloudFormation의 관계
