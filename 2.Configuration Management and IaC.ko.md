# 2. 구성 관리 및 Infrastructure as Code

> **DOP-C02 시험 비중: 17%**
> CloudFormation 템플릿 구조, StackSets, 드리프트 감지, CDK vs SAM 비교, OpsWorks 수명 주기가 핵심입니다.

---

## 구성 관리 (Configuration Management)

컴퓨터 시스템의 모든 구성 요소(소프트웨어, 하드웨어, 네트워킹)를 설정하고 관리하기 위한 포괄적이고 최신의 매뉴얼을 유지하는 것과 같습니다. AWS는 복원력 있고 확장 가능한 클라우드 환경을 지원하기 위해 자동화와 일관성을 강조합니다.

| 이점 | 설명 |
|---|---|
| **확장성** | 자동화된 구성으로 시스템이 원활하게 성장 |
| **신뢰성** | 일관된 설정으로 환경 간 오류 감소 |
| **재해 복구** | 사전 정의된 재현 가능한 구성으로 복원 간소화 |

### 2025년 개선 사항
- **관측성** — CloudWatch 및 X-Ray 통합으로 실시간 구성 모니터링
- **규정 준수** — AWS Config 자동 검사 → 제로 트러스트 보안 모델에 부합

---

## Infrastructure as Code (IaC)

IaC는 수동 프로세스나 대화형 도구 대신 **머신 리더블 파일**을 통해 데이터 센터를 관리하고 프로비저닝합니다. AWS는 안전성, 확장성, 멀티 계정 거버넌스를 강조하며 IaC를 기본 DevOps 실천 방법으로 강화합니다.

| 이점 | 설명 |
|---|---|
| 사람이 읽을 수 있는 구성 | 이해와 협업 간소화 |
| 변경 사항 미리보기 | 배포 전 인프라 업데이트 확인 |
| 미래 성장 확장성 | 모듈식 설계로 요구 사항 변화에 적응 |
| 계정·리전 간 관리 | 멀티 리전/멀티 계정 설정 간소화 |
| 종속성 관리 | 소프트웨어·리소스 종속성 효율적 처리 |
| 향상된 안전 제어 | 가드레일과 규정 준수 검사 통합 |

---

## AWS CloudFormation

CloudFormation은 AWS 인프라를 정의하기 위한 **선언적** 접근 방식을 제공하여 자동화, 버전 관리, 검토 가능한 변경을 가능하게 합니다.

### CloudFormation 구성 요소 (6가지)

> **시험 포인트**: 6가지 구성 요소 중 **Resources만 필수**이며 나머지는 선택입니다.

| 구성 요소 | 역할 | 예시 |
|---|---|---|
| **Resources** (필수) | 생성할 AWS 리소스 정의 | EC2, S3, RDS |
| **Parameters** | 배포 시 사용자 입력값 | 인스턴스 타입, 키 이름 |
| **Mappings** | 환경별 정적 변수 | 리전별 AMI ID |
| **Outputs** | 스택 결과 값 내보내기 | VPC ID, ALB DNS |
| **Conditions** | 조건부 리소스 생성 로직 | prod vs dev 분기 |
| **Metadata** | 추가 템플릿 세부 정보 | UI 그룹핑 |

### 주요 내장 함수 (Intrinsic Functions)

| 함수 | 용도 |
|---|---|
| `Fn::Ref` / `!Ref` | 파라미터 또는 리소스 참조 |
| `Fn::GetAtt` | 리소스 속성 값 가져오기 |
| `Fn::Join` | 문자열 결합 |
| `Fn::Sub` | 문자열 내 변수 치환 |
| `Fn::If` | 조건부 값 반환 |
| `Fn::Select` | 리스트에서 항목 선택 |
| `Fn::ImportValue` | 다른 스택의 Output 참조 |

### 중요한 개념

| 개념 | 설명 |
|---|---|
| **의사 파라미터** | 기본 AWS 값 — `AWS::AccountId`, `AWS::Region`, `AWS::StackName` |
| **드리프트 감지** | 템플릿 외 수동 변경(대역 외 변경)을 식별 |
| **삭제 정책** | `Retain` (보존), `Snapshot` (스냅샷 후 삭제), `Delete` (삭제) |
| **사용자 정의 리소스** | Lambda 기반 로직으로 비-AWS 리소스 관리 |
| **서비스 역할** | CloudFormation이 리소스를 관리하기 위한 IAM 역할 |
| **StackSets** | 멀티 계정·멀티 리전 스택 관리 |

> **시험 포인트**: StackSets는 AWS Organizations와 함께 사용하여 여러 계정/리전에 동시 배포할 때 출제됩니다.

### 배포 전략

| 전략 | 동작 |
|---|---|
| **AutoScalingReplacingUpdate** | 스케일링 그룹 전체를 교체 (주요 업데이트) |
| **AutoScalingRollingUpdate** | 인스턴스를 배치로 업데이트 (다운타임 최소화) |

### 변경 세트 (Change Sets)
배포 전에 변경 사항을 미리 확인할 수 있으며, 비용 및 지속 가능성 영향 분석이 포함됩니다. 규정 준수 시행을 위한 AWS Config Rules 가드레일도 통합됩니다.

---

## Service Catalog

CloudFormation 템플릿의 큐레이션된 세트를 관리하며 거버넌스를 간소화합니다. AWS Organizations와 통합되어 계정 간 중앙 집중식 정책 시행이 가능합니다.

---

## Elastic Beanstalk

EC2, ASG, ELB, RDS에서 애플리케이션을 배포하기 위한 상위 수준 서비스입니다. 다양한 배포 모델(웹 서버, 워커)을 지원합니다.

| 2025 기능 | 설명 |
|---|---|
| ECS 통합 | EB CLI를 통한 간소화된 컨테이너 배포 |
| X-Ray 지원 | 구성 문제 해결을 위한 향상된 추적 |

---

## AWS CDK vs SAM 비교

> **시험 포인트**: CDK와 SAM의 차이점, 사용 시나리오가 자주 출제됩니다.

| 항목 | AWS CDK | AWS SAM |
|---|---|---|
| **정의 방식** | TypeScript, Python 등 프로그래밍 언어 | YAML/JSON 템플릿 |
| **대상** | 모든 AWS 리소스 | 서버리스 특화 (Lambda, API GW, DynamoDB) |
| **내부 동작** | CloudFormation으로 변환 (synth) | CloudFormation으로 변환 |
| **로컬 테스트** | CDK assertions | SAM CLI `sam local` |
| **적합 시나리오** | 복잡한 인프라, 재사용 가능한 구성 요소 | 서버리스 앱 빠른 개발 |

---

## AWS Step Functions

워크플로를 상태 머신으로 오케스트레이션하며, EventBridge와 긴밀하게 통합되어 이벤트 기반 IaC 업데이트를 지원합니다.

| 유형 | 특징 |
|---|---|
| **Standard Workflows** | 장시간 실행 (최대 1년), 정확히 한 번 실행 |
| **Express Workflows** | 대용량·단기 작업 최적화 (최대 5분), 최소 한 번 실행 |

---

## AWS AppConfig & Systems Manager

| 서비스 | 역할 |
|---|---|
| **AppConfig** | 코드와 독립적으로 동적 구성 배포, 카나리 롤아웃 지원 |
| **Systems Manager** | 운영 데이터와 자동화 중앙 집중화, Parameter Store 암호화 (고객 관리형 KMS) |

### Systems Manager 주요 기능
- **자동화 런북** — 구성 복구를 위한 사전 구축 워크플로
- **규정 준수 검사** — AWS Config 규칙에 대한 실시간 검증

---

## OpsWorks

AWS OpsWorks는 관리형 **Chef** 및 **Puppet** 인스턴스를 제공하며 오토 스케일링 및 수명 주기 관리를 지원합니다. 하이브리드 클라우드 호환성 및 자동 복구를 강조합니다.

### OpsWorks 주요 기능

| 기능 | 설명 |
|---|---|
| **오토 스케일링** | 시간 기반 또는 부하 기반, ECS/Fargate 지원 |
| **수명 주기 이벤트** | Setup → Configure → Deploy → Undeploy → Shutdown |
| **자동 복구** | 실패한 인스턴스 자동 교체 |

> **시험 포인트**: OpsWorks의 5가지 수명 주기 이벤트 순서를 기억하세요.

### 기억해야 할 핵심 개념
- **AWS Application Discovery Service** — 마이그레이션 위해 온프레미스 VM 평가, AI 기반 추천
- **OpsWorks 수명 주기 후크** — 인스턴스 관리 작업을 위한 커스텀 스크립트
- **서비스 역할 및 권한** — 최소 권한 액세스를 위한 IAM 역할

### 2025년 업데이트
- **하이브리드 지원** — 온프레미스 IaC를 위한 AWS Outposts 통합 개선
- **모니터링** — OpsWorks 스택 성능을 위한 CloudWatch 메트릭
