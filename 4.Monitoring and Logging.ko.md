# 4. 모니터링 및 로깅

> **DOP-C02 시험 비중: 15%**
> CloudWatch(메트릭, 로그, 알람, Synthetics), CloudTrail(API 감사), Config(규정 준수), VPC Flow Logs 제외 항목이 핵심입니다.

---

## AWS Config

AWS Config는 AWS 리소스 구성을 추적, 감사, 평가하여 내부 IT 정책에 대한 규정 준수를 보장합니다.

| 기능 | 설명 |
|---|---|
| 구성 변경 모니터링 | 시간 경과에 따른 리소스 변경 추적 |
| 규정 준수 평가 | 사전 정의된 규칙 기준 자동 평가 |
| **제한** | 활성화된 리전에서만 모니터링 |

### 애그리게이터 (Aggregators)

여러 소스의 구성 및 규정 준수 데이터를 통합하여 통합 분석을 제공합니다. **멀티 리전/멀티 계정** 환경에서 중앙 집중식 데이터 수집에 사용합니다.

> **시험 포인트**: Config는 리전별 서비스이므로, 여러 리전/계정을 모니터링하려면 반드시 **Aggregator**를 사용해야 합니다.

---

## VPC 흐름 로그 (Flow Logs)

VPC 흐름 로그가 캡처하지 **않는** 트래픽:

> **시험 포인트**: "캡처되지 않는 트래픽" 목록은 단골 출제 항목입니다.

| 제외 트래픽 |
|---|
| 인스턴스 메타데이터 서비스(169.254.169.254) 트래픽 |
| Amazon Time Sync Service 트래픽 |
| DHCP 트래픽 |
| 기본 VPC 라우터 예약 IP 트래픽 |
| ELB ↔ ENI 간 트래픽 |
| Windows 라이선스 활성화 트래픽 |
| Amazon DNS 서버 트래픽 (자체 BIND DNS는 기록됨) |

---

## CloudTrail

AWS 계정 내 API 호출 및 이벤트를 감사·로깅하는 데 필수적인 서비스입니다.

| 기능 | 설명 |
|---|---|
| 이벤트 캡처 | 관리, 데이터, 네트워크 이벤트 |
| 조직 전체 추적 | AWS Organizations 통합 |
| 로그 무결성 | 암호화 해시 기반 **로그 파일 유효성 검사** 활성화 |

> **시험 포인트**: CloudTrail 로그 무결성 검증은 보안 감사 시나리오에서 자주 출제됩니다. 유효성 검사를 활성화하면 SHA-256 해시로 변조 여부를 확인합니다.

### 2025년 개선 사항
- EventBridge와 통합 → 실시간 이벤트 라우팅
- CloudWatch 통한 로그 패턴 AI 기반 이상 탐지

---

## CloudWatch

CloudWatch는 로그, 메트릭, 이벤트의 **실시간 모니터링**을 제공하며, 구독을 통해 Kinesis, Firehose, Lambda로 데이터를 전달합니다.

### CloudWatch 주요 기능 한눈에 보기

| 기능 | 설명 |
|---|---|
| **메트릭 스트림** | S3, 서드파티 도구로 메트릭 데이터 스트리밍 |
| **사용자 정의 메트릭** | `PutMetricData` API로 커스텀 메트릭 전송 (최소 1초 간격) |
| **이상 탐지** | ML로 메트릭 기준선 설정 → 편차 자동 표시 |
| **로그 그룹/스트림** | 로그를 카테고리별(그룹), 이벤트 시퀀스별(스트림)로 구성 |
| **Logs Insights** | 로그 데이터 검색·분석 쿼리 엔진 |
| **Synthetic Canaries** | API/URL/웹사이트 모니터링 스크립트 (가용성, 지연 시간, 스크린샷) |

### Amazon Lookout for Metrics

| 항목 | 설명 |
|---|---|
| 기능 | ML로 메트릭 이상 자동 감지 + 근본 원인 식별 |
| 통합 | AppFlow를 통해 AWS 서비스 및 SaaS 앱 연동 |
| 알림 | SNS, Lambda, Slack, Webhooks 지원 |

### CloudWatch Synthetic Canaries

> **시험 포인트**: Canary는 사용자 행동을 시뮬레이션하여 **사전에** 문제를 감지합니다. Blue/Green 배포의 검증 단계에서 자주 활용됩니다.

API, URL, 웹사이트를 모니터링하는 스크립트로 가용성, 지연 시간을 측정하고 UI 데이터(스크린샷 포함)를 캡처합니다. 멀티 리전 테스트와 지연 시간 최적화도 지원합니다.

---

## Amazon Athena

S3 데이터를 분석하기 위한 대화형 SQL 쿼리 서비스입니다.

| 특징 | 설명 |
|---|---|
| 서버리스 | 인프라 관리 불필요 |
| 과금 | 스캔한 데이터 양 기준 |
| 용도 | CloudTrail 로그, VPC Flow Logs, ELB 로그 분석에 자주 사용 |

> **시험 포인트**: "S3에 저장된 로그를 분석하려면?" → Athena가 정답인 경우가 많습니다.

---

## AWS Trusted Advisor

AWS 모범 사례를 기반으로 **5가지 카테고리**의 실시간 권장 사항을 제공합니다:

| 카테고리 | 설명 |
|---|---|
| **비용 최적화** | 절약 기회 식별 |
| **성능** | 효율성 개선 제안 |
| **보안** | 취약점 강조 |
| **내결함성** | 복원력 강화 |
| **서비스 한도** | 사용량 임계값 모니터링 |

### 서비스 한도 모니터링

| 항목 | 설명 |
|---|---|
| 추적 대상 | AWS 서비스 할당량 (스케일링 문제 방지) |
| 알림 | CloudWatch 알람 + Trusted Advisor 연동 |
| **주의** | `ServiceLimitUsage` 메트릭은 **Business/Enterprise** 지원 플랜 필요 |

> **시험 포인트**: Trusted Advisor의 전체 기능은 Business/Enterprise 플랜에서만 사용 가능하며, Basic/Developer 플랜은 일부 검사만 제공됩니다.
