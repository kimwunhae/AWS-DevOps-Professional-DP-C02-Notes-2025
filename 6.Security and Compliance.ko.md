# 보안 및 규정 준수

## 보안 기본 원칙
- 최소 권한(Least Privilege)
- 명시적 거부 우선
- 다계층 방어(Defense in Depth)
- 기본 암호화(Encryption by Default)

## IAM
- 사용자보다 역할(Role) 중심 설계
- 정책 조건(Condition) 적극 활용
- 권한 경계(Permission Boundary)와 SCP 병행
- 교차 계정 접근은 신뢰 정책 + 외부 ID 검토

## 데이터 보호
- KMS 고객 관리형 키(CMK) 사용
- S3 SSE-KMS, EBS 암호화, RDS 암호화
- 시크릿은 Secrets Manager/Parameter Store 사용

## 네트워크 보안
- 보안 그룹 최소 개방
- NACL/라우팅/서브넷 분리
- VPC Endpoint로 프라이빗 통신
- WAF/Shield/Firewall Manager 연계

## 탐지 및 대응
- CloudTrail 전 계정/전 리전 활성화
- GuardDuty, Security Hub, Config Rule 활용
- Macie로 민감정보 탐지
- 자동 격리/차단 런북 구성

## 컴플라이언스
- AWS Config로 구성 준수 점검
- Audit Manager로 증적 수집 자동화
- 규정별 기준(ISO, PCI-DSS, SOC) 매핑

## 시험 포인트
- IAM, KMS, CloudTrail 조합 문제 빈도 높음
- 보안과 운영 효율의 균형(자동화 + 거버넌스)
- 멀티 계정 환경에서 중앙 보안 관리 아키텍처 이해
