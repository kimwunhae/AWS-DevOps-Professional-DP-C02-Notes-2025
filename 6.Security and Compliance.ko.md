# 6. 보안 및 규정 준수

> **DOP-C02 시험 비중: 16%**
> IAM 정책/역할, Organizations(SCP), Control Tower(가드레일), Secrets Manager vs Parameter Store 비교, CodeBuild/CodeCommit 보안 모범 사례가 핵심입니다.

---

## AWS 관리 및 거버넌스 서비스 개요

| 서비스 | 역할 |
|---|---|
| **AWS Organizations** | 멀티 계정 거버넌스 중앙 집중화, 정책 기반 관리 |
| **AWS Service Catalog** | 승인된 IT 서비스 카탈로그 생성·관리 (VM, 서버, 소프트웨어, 다중 계층 아키텍처) |
| **AWS Systems Manager** | AWS + 온프레미스 인프라 통합 관리 (패치, 세션, 자동화, 파라미터) |
| **AWS Config** | 리소스 구성을 내부 정책/규제 표준에 대해 추적 → 규정 준수 자동 평가 |

---

## 보안 서비스

| 서비스 | 핵심 기능 | 데이터 소스 |
|---|---|---|
| **AWS Inspector** | EC2 취약성 자동 평가 + 보안 표준 시행 | SSM Agent 설치 필요 |
| **Amazon GuardDuty** | ML 기반 지능적 위협 탐지 | CloudTrail, VPC Flow Logs, DNS 로그 |
| **Amazon Macie** | S3 버킷에서 PII 탐지 | S3 데이터 (HIPAA, GDPR 규정 준수) |
| **AWS Trusted Advisor** | 비용/보안/성능/내결함성/서비스 한도 권장 | 계정 전반 |

> **시험 포인트**: GuardDuty가 분석하는 **3가지 데이터 소스**(CloudTrail, VPC Flow Logs, DNS 로그)를 기억하세요.

---

## CodeBuild 보안 모범 사례

| 항목 | 모범 사례 |
|---|---|
| 빌드 정의 | **buildspec 파일**로 빌드 명령 및 설정 정의 |
| 비밀번호 | 하드코딩 금지 → IAM 정책과 역할 사용 |
| 민감 데이터 | **SSM Parameter Store** 또는 **Secrets Manager**에 저장 |
| 명령 실행 | SSH 대신 **Systems Manager(SSM)** 사용 |

---

## CodeCommit 보안 모범 사례

> **시험 포인트**: IAM 정책 할당 기준을 묻는 시나리오가 자주 출제됩니다.

| 대상 | 정책 |
|---|---|
| 관리자 | `CodeCommitFullAccess` |
| 개발자 | `PowerUser` |
| 서드파티 | `ReadOnly` |
| 브랜치 보호 | `main`/`master` 직접 푸시 차단하는 커스텀 정책 |

---

## S3 버킷 정책 & AWS Config

> **시험 포인트**: S3에서 HTTPS를 강제하는 방법은 빈출 문제입니다.

| 단계 | 설명 |
|---|---|
| 1. 버킷 정책 | HTTP 요청에 `"Deny"` 효과 적용 (`"aws:SecureTransport": "false"`) |
| 2. Config 규칙 | S3 버킷이 SSL을 요구하는지 자동 검증 |

---

## Secrets Manager vs SSM Parameter Store

> **시험 포인트**: 두 서비스의 차이점은 거의 매 시험에 출제됩니다.

| 항목 | SSM Parameter Store | Secrets Manager |
|---|---|---|
| **비용** | 무료 (Standard), 유료 (Advanced) | 유료 |
| **자동 로테이션** | 없음 | 있음 (Lambda 기반) |
| **교차 계정 액세스** | 제한적 | 지원 |
| **비밀번호 생성** | 없음 | 자동 생성 가능 |
| **적합 용도** | 일반 구성 데이터, 간단한 시크릿 | DB 자격 증명, API 키 (로테이션 필요) |

---

## AWS 관리형 정책

| 정책 | 권한 범위 |
|---|---|
| **AdministratorAccess** | 모든 AWS 서비스 전체 액세스 |
| **PowerUserAccess** | IAM 관리 제외 광범위한 액세스 |
| **ReadOnlyAccess** | 보기 전용 |

---

## AWS Systems Manager (SSM)

SSM Agent를 통해 EC2 인스턴스 및 온프레미스 머신 관리를 중앙 집중화합니다.

| 기능 | 설명 |
|---|---|
| **Inventory** | 관리형 인스턴스 메타데이터 수집 (설치 앱, OS 버전 등) |
| **Patch Manager** | 커스텀 패치 기준선 정의 + 유지 관리 기간으로 업데이트 스케줄링 |
| **Session Manager** | SSH 없이 안전한 셸 액세스 |
| **Automation** | 사전 정의된 런북으로 운영 작업 자동화 |
| **Parameter Store** | 구성 데이터 및 시크릿 저장 |

---

## AWS Config (상세)

| 기능 | 설명 |
|---|---|
| 감사·추적 | AWS 리소스 규정 준수 기록 |
| 변경 알림 | 구성 변경 시 알림 전송 |
| 리전 범위 | 리전별 운영, 계정/리전 간 집계 가능 |
| 데이터 저장 | S3에 저장 → **Amazon Athena**로 쿼리 |
| 통합 | CloudTrail(API 추적) + SSM Automation(복구) |

---

## AWS Organizations

AWS 계정 및 리소스 거버넌스를 중앙 집중화합니다. 멤버 계정에 `OrganizationAccountAccessRole` IAM 역할을 생성합니다.

### 서비스 제어 정책 (SCPs)

> **시험 포인트**: SCP의 적용 범위와 제한을 정확히 이해해야 합니다.

| 항목 | 설명 |
|---|---|
| 정의 | OU 또는 계정 수준에서 IAM 작업 허용/차단 목록 |
| 적용 대상 | 모든 사용자와 역할 (루트 포함) |
| **미적용** | 관리 계정, 서비스 연결 역할 |
| 기본 동작 | 명시적 허용 필요 → 권한은 기본 거부 |

---

## AWS Control Tower

안전하고 규정을 준수하는 멀티 계정 AWS 환경의 설정·거버넌스를 간소화합니다.

| 기능 | 설명 |
|---|---|
| 계정 프로비저닝 | AWS Organizations + Service Catalog로 자동화 |
| **예방 가드레일** | SCP를 통해 시행 |
| **탐지 가드레일** | AWS Config로 모니터링 |
| Config 통합 | 모든 리전에서 자동으로 Config 활성화, 로그 아카이브 계정에 중앙 집중화 |
| **AFT** | Terraform으로 AWS 계정 프로비저닝·사용자 정의 |

> **시험 포인트**: 가드레일의 두 유형 — **예방(Preventive, SCP)** vs **탐지(Detective, Config)** — 을 구분하세요.

---

## IAM Identity Center (SSO)

| 기능 | 설명 |
|---|---|
| 단일 로그인 | 조직의 모든 AWS 계정 단일 포털 |
| 외부 IdP 통합 | Active Directory 등 지원 |
| 권한 관리 | 권한 세트 + ABAC (속성 기반 액세스 제어) |

---

## Web Application Firewall (WAF)

| 항목 | 설명 |
|---|---|
| 보호 대상 | SQL 인젝션, XSS 등 웹 익스플로잇 |
| 배포 위치 | ALB, API Gateway, CloudFront, AppSync |
| 규칙 | 관리형 규칙 세트 제공 |

---

## AWS Firewall Manager

AWS 조직 내 계정 간 보안 규칙(WAF, 보안 그룹)의 관리를 중앙 집중화합니다.

---

## Amazon GuardDuty

| 항목 | 설명 |
|---|---|
| 기능 | ML 기반 지능적 위협 탐지 |
| 분석 대상 | CloudTrail, VPC Flow Logs, DNS 로그 |
| 활성화 | 원클릭 30일 무료 평가판 |
