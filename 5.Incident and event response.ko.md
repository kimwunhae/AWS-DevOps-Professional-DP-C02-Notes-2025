# 5. 인시던트 및 이벤트 대응

> **DOP-C02 시험 비중: 15%**
> EventBridge 이벤트 라우팅, S3/RDS 이벤트 알림, 자격 증명 노출 대응 워크플로, CodePipeline 이벤트 오케스트레이션이 핵심입니다.

---

## AWS EventBridge

AWS EventBridge는 AWS 리소스의 변경에 반응하여 워크플로를 자동화하는 **서버리스 이벤트 버스**입니다. AWS 서비스, 커스텀 애플리케이션, SaaS 파트너 서비스의 이벤트를 Lambda, SNS, SQS 등의 대상으로 라우팅합니다.

> **시험 포인트**: EventBridge는 CloudWatch Events의 후속 서비스입니다. 동일한 기본 API를 공유하지만, EventBridge가 더 넓은 통합과 고급 기능을 제공합니다.

| 특징 | 설명 |
|---|---|
| 서비스 통합 | 200+ AWS 서비스, 35+ SaaS 파트너 |
| 이벤트 스키마 | 표준화된 이벤트 처리 |
| AI 패턴 매칭 | Amazon Q로 규칙 생성 가속화 |
| 멀티 리전 라우팅 | 리전 간 효율적 이벤트 분배 |

---

## Amazon S3 이벤트 알림

S3 버킷 내 특정 활동에 대한 알림으로 자동 응답을 트리거합니다.

| 이벤트 유형 | 트리거 |
|---|---|
| **객체 생성** | PUT, POST, COPY, 멀티파트 업로드 완료 |
| **객체 삭제** | 영구 삭제, 버전 지정 삭제 |
| **객체 복원** | Glacier / Deep Archive에서 복원 |
| **이중화 손실** | 내구성 감소 이벤트 |
| **복제 적격성** | S3 Replication Time Control(RTC) 기준 충족 |

### 2025년 업데이트
- **태그 기반 이벤트 필터링** — 객체 태그로 알림 필터링 (규정 준수 + 비용 관리)
- **EventBridge 통합** — SNS/Lambda 외에 다양한 대상으로 라우팅 확장

> **시험 포인트**: S3 이벤트 알림의 대상은 **SNS, SQS, Lambda**이며, EventBridge를 통하면 더 다양한 서비스로 라우팅 가능합니다.

---

## Amazon RDS 이벤트 알림

Amazon RDS는 주요 데이터베이스 이벤트에 대해 **Amazon SNS**를 통해 알림을 전달합니다.

| 이벤트 카테고리 | 예시 |
|---|---|
| **인스턴스 수명 주기** | 생성, 수정, 재시작, 삭제 |
| **리소스 알림** | 낮은 스토리지 임계값 |
| **구성 업데이트** | 인스턴스 설정 변경 |
| **고가용성** | Multi-AZ 장애 조치 이벤트 |

### 2025년 개선 사항
- **실시간 장애 조치 알림** — X-Ray 추적으로 빠른 근본 원인 분석
- **멀티 리전 집계** — EventBridge로 리전 간 이벤트 데이터 통합

---

## 노출된 자격 증명 위험 처리

> **시험 포인트**: 자격 증명 노출 대응 워크플로(EventBridge → Step Functions → Lambda → SNS)는 실전 시나리오로 자주 출제됩니다.

AWS Step Functions는 EventBridge 또는 CloudWatch 규칙과 결합하여 자동화된 복구 워크플로를 오케스트레이션합니다. 개발자가 실수로 커밋한 노출된 AWS 자격 증명(ACCESS KEY, Secret, 비밀번호)을 철회하는 데 특히 유용합니다.

### 워크플로 예시

```
1. EventBridge → CodeCommit 푸시에서 자격 증명 노출 감지
2. Step Functions → Lambda 함수 트리거 → 노출된 키 비활성화
3. SNS → 보안 팀에 알림 전송
```

### 2025년 기능
- **AI 기반 탐지** — ML로 로그에서 자격 증명 패턴 식별
- **제로 트러스트 시행** — 탐지 후 MFA 요구로 액세스 정책 자동 업데이트

---

## CodePipeline에서의 이벤트 오케스트레이션

CodePipeline은 다음 파라미터로 이벤트 기반 자동화를 관리합니다:

| 파라미터 | 역할 |
|---|---|
| **`runOrder`** | 파이프라인 스테이지 내 액션 순서 정의 (예: Lambda 호출 순서) |
| **`pollForSourceChanges`** | 소스(CodeCommit, S3, GitHub)의 업데이트 폴링 제어 |

> **시험 포인트**: `pollForSourceChanges: false`로 설정하고 **EventBridge**로 대체하는 것이 모범 사례입니다. 폴링 대신 실시간 이벤트 트리거가 권장됩니다.

### 사용법
- 소스 변경에 따라 파이프라인 실행을 자동화
- Lambda 함수로 워크플로를 사용자 정의

---

## AWS Distro for OpenTelemetry (ADOT)

ADOT는 애플리케이션과 서비스에서 **메트릭, 트레이스, 컨텍스트 데이터**를 수집하여 CloudWatch, X-Ray 또는 서드파티 관측성 플랫폼으로 전송합니다. 분산 시스템의 디버깅과 이해를 향상시킵니다.

| 항목 | 설명 |
|---|---|
| **이점** | 여러 프로그래밍 언어 계측 간소화, 포괄적 관측성 |
| **트레이스 샘플링** | 비용 효율성 위해 데이터 수집 최적화 |
| **Synthetics 통합** | 애플리케이션 성능 엔드투엔드 모니터링 |
