# 3. 복원력 있는 클라우드 솔루션

> **DOP-C02 시험 비중: 15%**
> HA vs FT 차이, DR 전략(비용/RTO 비교), Route 53 라우팅 정책, Lambda 동시성을 중점 학습하세요.

---

## HA vs 내결함성

> **시험 포인트**: HA와 FT의 차이는 자주 출제됩니다. "다운타임 최소화" vs "기능 손실 제로"로 구분하세요.

| 구분 | HA (고가용성) | FT (내결함성) |
|---|---|---|
| **목표** | 최소한의 다운타임 | 기능 손실 제로 |
| **초점** | 가동 시간 연속성 | 장애 시에도 완전 작동 |
| **구현** | 멀티 AZ, 멀티 리전 | AI 기반 자가 복구, 중복 구성 요소 |
| **비용** | 상대적으로 낮음 | 높음 (이중화 필요) |

---

## 멀티 AZ vs 멀티 리전

| 구분 | 멀티 AZ | 멀티 리전 |
|---|---|---|
| **범위** | 하나의 리전 내 격리된 가용 영역 | 지리적으로 분산된 여러 리전 |
| **목적** | 로컬 장애 시 다운타임 최소화 | 재해 복원력 + 지연 시간 최적화 |
| **비용** | 상대적으로 낮음 | 높음 |
| **사용 사례** | RDS Multi-AZ, ELB | Route 53, S3 CRR, Aurora Global |

---

## RTO 및 RPO

> **시험 포인트**: DR 전략별 RTO/RPO 범위를 알고 있어야 합니다.

| 지표 | 의미 | 핵심 질문 |
|---|---|---|
| **RTO** (복구 시간 목표) | 장애 후 서비스 복원까지 걸리는 시간 | "얼마나 빨리 복구해야 하는가?" |
| **RPO** (복구 시점 목표) | 최대 허용 가능한 데이터 손실 (시간) | "얼마만큼의 데이터 손실을 감수할 수 있는가?" |

---

## Route 53 라우팅 정책

> **시험 포인트**: 시나리오에 맞는 라우팅 정책을 선택하는 문제가 자주 나옵니다.

| 정책 | 동작 | 사용 시나리오 |
|---|---|---|
| **Simple** | 단일 리소스로 트래픽 전달 | 기본적인 웹사이트 |
| **Failover** | 액티브-패시브 설정 | DR (주/보조 환경) |
| **Geolocation** | 사용자 위치 기반 라우팅 | 규정 준수, 지역별 콘텐츠 |
| **Geoproximity** | 근접성 기반 + 바이어스 조정 | 트래픽 분배 미세 조정 |
| **Latency** | 최저 지연 시간 엔드포인트로 전달 | 글로벌 앱 성능 최적화 |
| **Multivalue Answer** | 최대 8개 정상 레코드 무작위 반환 | 간단한 부하 분산 |
| **Weighted** | 가중치별 트래픽 할당 | A/B 테스트, 단계적 롤아웃 |

### 2025년 업데이트
- 모든 정책에 CloudWatch Synthetics 상태 확인 통합
- 지속 가능성 인식 라우팅이 저탄소 리전 우선

---

## 오토스케일링 수명 주기 후크

| 후크 | 동작 |
|---|---|
| **Pending:Wait** → **Pending:Proceed** | 인스턴스 시작 일시 중지 → 커스텀 작업 수행 |
| **Terminating:Wait** → **Terminating:Proceed** | 종료 일시 중지 → 정리 작업 수행 |

일시 중지 중에 **EventBridge, Lambda, SNS, SSM**과 통합하여 알림, 스크립트, 검증을 수행합니다.

---

## Karpenter

Kubernetes 클러스터에서 노드 프로비저닝을 자동화하는 AWS 오픈 소스 도구입니다. EKS와 통합되어 스팟 인스턴스 최적화 및 비용 절감을 지원합니다.

---

## Amazon EKS 네트워킹

EKS 배포는 **제어 플레인 VPC** (AWS 관리형)와 **사용자 VPC**를 생성합니다. EKS 관리형 ENI가 내부 VPC 통신을 가능하게 합니다.

### EKS 네트워킹 애드온

| 애드온 | 역할 |
|---|---|
| **Amazon VPC CNI** | 파드에 프라이빗 IP 할당, IPv6 지원 |
| **AWS Load Balancer Controller** | ALB/NLB 관리, 지연 시간 기반 라우팅 |
| **CoreDNS** | 파드 이름 해석, 대규모 클러스터 최적화 |
| **Kube-proxy** | 네트워크 규칙 시행, X-Ray 관측성 |
| **Calico** | 네트워크 정책, 제로 트러스트 기본값 |

---

## Amazon S3 객체 태깅 및 액세스 제어

태그로 S3 객체를 분류(예: `DataClassification`, `Owner`)하여 IAM 정책을 통해 액세스를 관리합니다. 태그 기반 수명 주기 정책으로 비용 최적화가 가능합니다.

---

## Amazon Storage Gateway

| 유형 | 용도 |
|---|---|
| **S3 File Gateway** | 온프레미스 앱 → S3 연결 |
| **FSx File Gateway** | 온프레미스 → FSx 파일 공유 액세스 |
| **Tape Gateway** | 물리적 테이프 → S3 기반 가상 테이프 대체 |
| **Volume Gateway** | S3 스냅샷 포함 iSCSI 블록 스토리지 |

---

## AWS Lambda 버전 & 별칭

| 개념 | 설명 |
|---|---|
| `$LATEST` | 작업 중인 편집 가능한 버전 |
| **버전** | 게시 후 불변 스냅샷 |
| **별칭** | 버전 포인터 → 가중치로 카나리 배포 가능 |

> **참고**: 별칭은 고유한 ARN을 가지지만 직접 호출할 수 없습니다. 트래픽 전환에 사전/사후 후크를 사용할 수 있습니다.

---

## Lambda 동시성

> **시험 포인트**: 동시성 한도 초과 시 동기식 vs 비동기식 호출 동작 차이가 출제됩니다.

| 항목 | 값/동작 |
|---|---|
| **기본 한도** | 리전당 1,000개 동시 실행 (조정 가능) |
| **예약된 동시성** | 특정 함수에 동시성 한도 할당 → 초과 시 스로틀링 |
| **동기식 초과** | HTTP 429 (Too Many Requests) 반환 |
| **비동기식 초과** | DLQ (Dead Letter Queue)로 라우팅 |

---

## API Gateway

### 엔드포인트 유형

| 유형 | 특징 |
|---|---|
| **엣지 최적화** (기본값) | CloudFront 사용, 글로벌 도달 |
| **리전** | 로컬 리전 액세스 대상 |

### 보안 & 배포 스테이지
- HTTPS, 커스텀 도메인, 인증(IAM, Cognito) 지원
- **스테이지 변수**로 코드 변경 없이 환경 설정 전환

---

## ECS & EKS 로깅

| 서비스 | 로깅 방식 |
|---|---|
| **ECS** | `awslogs` 드라이버 또는 사이드카 컨테이너 |
| **EKS** | 제어 플레인 → CloudWatch, 노드/파드 → Fluent Bit / Fluentd |

CloudWatch Container Insights로 통합 로깅 가능.

---

## Kinesis

실시간 데이터(로그, 메트릭, 클릭스트림, IoT) 처리:

| 서비스 | 용도 |
|---|---|
| **Data Streams** | 원시 스트리밍 수집 |
| **Data Firehose** | 스트림 → 스토리지(S3, Redshift 등) 전달 |
| **Data Analytics** | 실시간 SQL 분석 |
| **Video Streams** | 비디오 수집·처리 |

---

## 재해 복구 전략 비교

> **시험 포인트**: 4가지 DR 전략의 비용/RTO 순서를 반드시 외우세요.

| 전략 | RTO | 비용 | 설명 |
|---|---|---|---|
| **Backup & Restore** | 높음 (시간~일) | 최저 | 백업 데이터에서 복원 |
| **Pilot Light** | 중간 (분~시간) | 낮음 | 최소 핵심 시스템만 가동, 나머지 필요 시 확장 |
| **Warm Standby** | 낮음 (분) | 중간 | 축소된 전체 시스템 상시 가동, 필요 시 스케일업 |
| **Multi-Site / Hot Site** | 최저 (초~분) | 최고 | 전체 프로덕션 이중화 상시 가동 |

---

## 백업, HA, 복제, 자동화 종합

| 범주 | 주요 서비스/기능 |
|---|---|
| **백업** | EBS 스냅샷, RDS 자동 백업, S3 버전 관리 |
| **고가용성** | Route 53, RDS Multi-AZ, 사이트 간 VPN |
| **복제** | RDS 읽기 전용 복제본, S3 CRR, Storage Gateway 동기화 |
| **자동화** | CloudFormation, EC2 자동 복구, Lambda 트리거, Step Functions |

---

## 카오스 엔지니어링

Netflix의 Simian Army는 장애를 주입하여(예: EC2 인스턴스 종료) 복원력을 테스트하는 카오스 엔지니어링의 대표적인 사례입니다.

**AWS Fault Injection Simulator (FIS)** — 실시간 카오스 모니터링을 위해 CloudWatch와 통합됩니다.

> **시험 포인트**: FIS는 제어된 장애 주입 실험을 통해 워크로드 복원력을 사전에 검증하는 관리형 서비스입니다.
